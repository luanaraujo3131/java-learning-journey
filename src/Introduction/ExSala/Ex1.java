/**
 * Escreva um programa que receba os três coeficientes(a, b, c) de uma equação de segundo grau
 * (ax^2 + bx + c = 0. O programa deve informar se a equação possui uma, duas ou nenhuma raiz
 * real, e caso existam, dizer quais são.
 * Para determinar isswo, calcule o valor de delta(delta = b^2 - 4ac). Se delta < 0, não há raizes
 * reais; se delta = 0, há uma única raiz real (x = -b/(2a)); e se delta > 0, há duas raízes reais
 * (x' = (-b+/delta(raiz de delta))/(2a)) e x'' = (-b-/delta(raiz de delta)) / (2a)).
 */

package Introduction.ExSala;

public class Ex1 {
}
