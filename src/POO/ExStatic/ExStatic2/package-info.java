package POO.ExStatic.ExStatic2;
//Same progam, but with specific tasks to specific methods;
//In this example, I won't use static methods, so to call an object(util/Calculator) I have to create a new
//object on Main:
// Calculator calc = new Calculator()